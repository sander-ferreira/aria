import RecommandationsCarousel from "@/components/recommandations-carousel";

/**
 * "Quelle IA utiliser ?" — carrousel des outils recommandés / à éviter.
 * Dernier écran de contenu du sous-module Dangers avant le badge.
 */
export default function DangersRecommandationsPage() {
  return <RecommandationsCarousel />;
}
